image:
  repository: todofrontend
  pullPolicy: Never
  tag: latest

service:
  port: 80

backend:
  service:
    port: 8080
  image:
    repository: todobackend
    tag: "latest"
  # Додаткові overrides якщо потрібно:
  # persistence:
  #   enabled: true
