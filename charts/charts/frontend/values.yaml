image:
  repository: todofrontend
  pullPolicy: Never
  tag: latest

service:
  port: 80

backend:
  service:
    port: 8080
  image:
    repository: todobackend
    tag: "latest"
  persistence:
    enabled: true

#frontend:
#  env:
#    BACKEND_URL: "http://todo:8080"

#commonBackendUrl: &backendUrl "http://todo:8080"

#frontend:
#  env:
#    BACKEND_URL: *backendUrl
