image:
  repository: todofront
  #tag: latest
  pullPolicy: Never #IfNotPresent

service:
  type: ClusterIP
  port: 80

apiUrl: "http://todoapp:8080"

ingress:
  enabled: true
  host: todofrontend.local
  port: 80
  annotations:
    className: "nginx"
    #kubernetes.io/ingress.class: nginx
